# Git Ignore Loader

This gitignore loader makes use of [TopTal's gitignore generator](https://www.toptal.com/developers/gitignore).

## Requirements

|Name|`pip` command|
|-|-|
|`requests`|`pip install requests`|